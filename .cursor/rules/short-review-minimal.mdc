---
description: 短篇/改稿验收默认只列必改项，改前先出方案征求确认；非强制
globs: shorts/**,novels/**,notes/**/WORKFLOW*.md
alwaysApply: false
---

# 验收与改稿：最小必要改动（偏好，非强制）

本规则是**默认偏好**，不是硬性流程。用户说「全改」「直接润色」「不用 review」时以用户为准。

与 `plan-then-execute` 配合：凡涉及改文件、跑 LLM 脚本落盘，**必须先出方案、等确认**。

## 适用场景

- `@story-review` / 多角色 solo 验收
- `check-ai-patterns`、`story-deslop` 等 AI 味检测
- 短篇 `final/`、长篇 `chapters/` 定稿前检查

## 第一阶段：只报告，不改文

输出三档清单：

| 档位 | 标准 | 默认处置 |
|---|---|---|
| **必改** | 逻辑穿帮、拒稿格式、时间线矛盾、关键事实错误 | 列入待修方案 |
| **建议** | 改了更好，不改也能上架（章节名、略糖、个别 AI 句） | **默认不执行** |
| **不动** | 狠话、短句节奏、口语、略粗糙但有人味的段 | 明确标注保留 |

报告末尾只汇总 **必改**（通常 3–8 条）。建议项单独列，供勾选。

**禁止**在此阶段：全篇润色、批量 Gemini polish、为「清零 AI 味」大改 prose。

## 第二阶段：出方案，征求意见

必改项确认前，用简短结构回复：

1. **目标** — 修哪几条、不动哪几条
2. **步骤** — 手改 / 局部脚本 / 哪几个 `###` 节
3. **风险** — 是否动 LOCK、是否耗 API、会否改平文气
4. **待你选** — 必改是否全做；建议项勾选 A/B/C

结尾问：**「按这个执行吗？」** 或 **「必改全做，建议只勾 X？」**

## 第三阶段：确认后才改

- 只动已确认的项，**局部替换**，不做全篇 pass
- 改完可再「只检测」验证必改归零；**不要求** AI 脚本项清零
- 若用户只要报告、不要改：停在第一阶段

## 必改 vs 建议（示例）

**必改**：对话缺「」、### 编号缺失、保释金前后矛盾、区块链/删帖时间轴冲突  
**建议**：章节名钩子化、反派反扑加码、男主糖精微调  
**不动**：狗咬狗互撕、一句一段黑岩体、情绪宣泄短句

## 用户快捷指令

- 「先 review，只列必改」→ 只第一阶段
- 「review 全列，只执行必改」→ 一、二阶段，方案默认不含建议项
- 「建议项勾 X、Y 再改」→ 方案含勾选，确认后执行

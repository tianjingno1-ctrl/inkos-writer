---
description: 用户交代任务时先出方案、确认后再执行（不写文件、不跑命令）
alwaysApply: true
---

# 先方案、后执行

用户说「要干嘛」「帮我做 X」「跑一下 Y」等**有执行意图**的指令时，默认走两阶段，不要直接开干。

## 第一阶段：只出方案（禁止执行）

在用户确认前，**不要**：
- 改/写/删文件
- 跑会改状态的命令（`write next`、`revise`、`reviseFoundation`、git commit、安装依赖等）
- 调 LLM 脚本落盘

可以先做**只读**摸底（读文件、查状态、`git status`），若对方没说清范围。

方案用简短结构，一般包含：

1. **目标** — 一句话
2. **步骤** — 2–5 条，标会动哪些文件/命令
3. **风险或取舍** — 如有（改地基、动 LOCK、耗 API）
4. **需要你确认的点** — 选项或默认建议

结尾明确问：**「按这个执行吗？」** 或给出 A/B 选项。

## 第二阶段：确认后再执行

用户回复「好」「执行」「按 A」「开始」等明确同意后，再跑命令、改文件。

若用户**只改方案**（「别用 GLM」「先只修 ch1」），更新方案并再次确认，不要半套半执行。

## 例外（可直接做，但仍要简短说明）

- 纯问答、解释概念、读文件回答问题
- 用户明确说「直接做」「不用确认」「你看着办」
- 用户消息本身就是对上一版方案的确认（「就按你说的」「第一步合并 volume_map」）

## 本书（年代军婚）示例

用户：「帮我修第 1 章」

应先回复方案，例如：
- 用 `reviser --mode polish` + Gemini 2.5 Pro
- 极短段排版、双引号、活世界侧写
- 不动 ch1 LOCK 节拍与年代设定
- 问：是否执行

用户确认后再跑 `revise chapter`。

---
description: 改正文只做用户点名的范围；禁止擅自压字与删完再补
globs: books/**/chapters/*.md
alwaysApply: true
---

# 改正文范围铁律（禁擅自压字 / 禁删完再补）

适用于 `books/*/chapters/*.md` 的手改、P0/P1 修补、定稿前修改。

## 范围跟用户走

- 用户说「改 P0」「只修 XX」时：**只改清单里的项**
- 用户**没写**「压字数 / 删到 N 字 / 砍长」→ **禁止**为凑目标字数而删戏、砍段、整章重写缩水
- 用户**没写**「补字 / 扩写 / 加厚」→ **禁止**在删减后再注水回补

## 禁止「删完了又补」

- **禁止**先大幅删正文，再用新写的闲笔、旁支、空转描写把字数补回去
- 若某次修改导致衔接断了：应**回退到修改前版本做外科修补**，不要用新段落硬接
- 补字必须接得上上下文（承接上一句动作/对话/物件），禁止无因果的清单句、复读句

## 字数怎么处理

- 改完仍须按 `chapter-wordcount-report.mdc` **汇报** `zh_chars`
- 字数偏高/偏低：在回复里**报告**即可；要压或要补，**单独进方案问用户**，确认后再动
- P0（硬伤：时间线、全知、禁词、错记接缝、引号等）**默认不包含压字**

## 正确做法（P0 示例）

1. 以当前稿（或 CLI 原稿）为底  
2. 逐条替换/删除 P0 句  
3. **保留**其余情节与篇幅  
4. 汇报字数；若仍超标，问要不要另开「压字」方案  
